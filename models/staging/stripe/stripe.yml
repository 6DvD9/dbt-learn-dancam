version: 2

sources:
  - name: stripe
    database: raw
    tables:
      - name: payment
     
models:
    - name: stg_payments
        description: oen records per paymentid per status per typle
        columns:
          - name: payment_id
            tests:
             - unique
             - not_null